# Cookies

We use **essential** cookies only: the Better Auth session cookie, plus `locations-theme` and `locations-mood` in localStorage (not cookies).

No advertising cookies. If we add analytics later, this page will list them.
