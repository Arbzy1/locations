# Cookies

We use **essential** cookies only: the Better Auth session cookie and `locations-theme` in localStorage (not a cookie).

No advertising cookies. If we add analytics later, this page will list them.
