# Subprocessors

Update this list when vendors change. Linked from the Privacy Policy.

| Processor | Purpose |
|-----------|---------|
| Cloudflare | Worker, DNS, R2, WAF |
| Neon | Postgres |
| Stripe | Payments |
| Resend | Transactional email |
| Map tile / routing / geocode vendor | Basemap, OSRM, reverse geocode (see env) |

Google Fonts should be self-hosted or dropped so CSP can stay strict.
