# Local Wrangler secrets (gitignored). Copy from .env.example values.
# Optional: `.dev.vars.staging` if you run `wrangler dev --env staging` against remote bindings.
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
BETTER_AUTH_SECRET=dev-secret-change-me-to-a-long-random-string
BETTER_AUTH_URL=http://localhost:8787
